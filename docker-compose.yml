version: '2'

services:
  nginx:
    build: docker/nginx
    ports:
      - 80:80
    volumes:
    - ./:/app
    networks:
      - app

  php:
    build: docker/php
    volumes:
      - ./:/app
    networks:
      - app

networks:
  app:
